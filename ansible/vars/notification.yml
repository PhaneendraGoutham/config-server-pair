ssh_port: "{{ hostvars[groups['notification'][0]]['ansible_ssh_port'] }}"
private_key_file: "{{ hostvars[groups['notification'][0]]['ansible_ssh_private_key_file'] }}"
profile: "{{ hostvars[groups['notification'][0]]['ansible_profile'] }}"
cur_dir: "{{ lookup('env','PWD') }}"
user: "{{ hostvars[groups['notification'][0]]['ansible_ssh_user'] }}"
jar_name: "notification.jar"
conf_name: "application.yml"
app_path: "/opt/notification"
app_conf_path: "{{ app_path }}/conf"
app_bin_path: "{{ app_path }}/bin"
project_name: "notification"
systemd_script_name: "notification.service"