spring:
  application:
    name: auth
  cloud:
    config:
      enabled: true
      label: ${CONFIG_BRANCH:master}
      uri: http://config:8888
      fail-fast: true
#      password: ${CONFIG_SERVICE_PASSWORD}
#      username: user